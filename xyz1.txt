sudo apt install

sudo apt-get update

sudo apt-get install docker.io
same but -getno
 
git clone path

ls
cd iacsdrepomarch23
ls
sudo docker build -t iacsd .

sudo docker images
sudo docker run -d -p 8081:8081 iacsd
sudo docker ps

